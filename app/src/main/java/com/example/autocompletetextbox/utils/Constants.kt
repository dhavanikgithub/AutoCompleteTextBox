package com.example.autocompletetextbox.utils

class Constants {
    companion object{
        const val BASE_URL = "https://raw.githubusercontent.com/"
        const val GITHUB_USERNAME = "dhavanikgithub"
        const val GITHUB_REPO_NAME = "country-flags"
        const val GITHUB_REPO_BRANCH = "main"
        const val MAIN_URL = "${BASE_URL}${GITHUB_USERNAME}/${GITHUB_REPO_NAME}/${GITHUB_REPO_BRANCH}"
    }
}